 <h1><i class="fas fa-edit">Edit Seminar</i></h1>
 
 <%= render 'form', seminar: @seminar %>

<%= link_to 'Delete Seminar', user_seminar_path(@seminar), method: :delete %>
<%= link_to 'Seminar index', user_seminars_path(current_user) %>
