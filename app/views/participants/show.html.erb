<h1><i class="fas fa-users">Participant</i></h1>

<p>
  <strong>Name:</strong>
  <%= @participant.name %>
</p>

<p>
  <strong>Membership_number:</strong>
  <%= @participant.membership_number %>
</p>

<p>
  <strong>Email:</strong>
  <%= @participant.email %>
</p>

<%= link_to 'Seminar', user_seminar_path(current_user, @seminar) %>

<%= link_to 'Edit', edit_user_seminar_participant_path(current_user, @seminar, @participant) %>
