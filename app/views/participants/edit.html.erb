<h1>Edit Parcitipant</h1>

<%= render 'form', participant: @participant %>

<%= link_to 'Seminar', user_seminar_path(current_user, @seminar) %> |
<%= link_to 'Destroy Participant', user_seminar_participant_path(current_user, @seminar, @participant), method: :delete %>
