<h1>Participants</h1>

<table>
  <thead>
    <tr>
      <th>name</th>
      <th>Membership_numer</th>
      <th>Email</th>
    </tr>
  </thead>

  <tbody>
    <% @participants.each do |participant| %>
      <tr>
        <td><%= participant.name %></td>
        <td><%= participant.membership_number %></td>
        <td><%= participant.email %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Participant', new_user_seminar_participant_path() %>
